## 1. 场景与资源骨架

- [ ] 1.1 创建 `Game` Unity 场景，并加入局内相机、舞台挂载根节点和 UI 打开入口所需基础对象
- [ ] 1.2 确认 `Game` 场景不硬编码具体关卡、怪物或卡牌数据，只作为局内容器
- [ ] 1.3 创建或整理局内场景资源路径，确保后续流程可以按项目资源管理约定引用 `Game` 场景和局内 Prefab

## 2. GameView UI 骨架

- [ ] 2.1 创建 `GameView`、`GameController` 及对应 UI Prefab，遵守现有 EF MVC UI 结构
- [ ] 2.2 在 `GameView` Prefab 中建立玩家状态区、手牌区、拖拽层、目标线层、结束回合按钮和弹窗根节点
- [ ] 2.3 为 `GameView` 提供最小可见占位内容，便于进入场景后人工验证布局和层级
- [ ] 2.4 确认 `GameView` 只负责表现和只读数据访问，用户输入通过 `GameController` 或后续输入接口发起

## 3. 战斗舞台 Prefab

- [ ] 3.1 创建 `BattleStage` 2D 世界空间预制体，包含 `BackgroundRoot`、`MonsterSlotRoot`、`WorldFxRoot` 和 `ForegroundRoot`
- [ ] 3.2 在背景根节点下创建远景、中景、近景三类占位层，并配置可观察的层级顺序
- [ ] 3.3 在怪物槽位根节点下创建 3x3 槽位锚点，按后排、中排、前排命名和排列
- [ ] 3.4 为怪物槽位配置位置、缩放和排序约定，使前排在视觉上能遮挡或覆盖后排
- [ ] 3.5 在舞台中预留世界特效根节点和前景根节点，供后续攻击、受击、环境遮挡使用

## 4. 表现脚本与连接

- [ ] 4.1 创建战斗舞台表现脚本，用于定位背景层、怪物槽位、特效根节点和前景根节点
- [ ] 4.2 创建背景滚动表现脚本，支持触发一次短暂前进滚动动画，并允许不同背景层使用不同滚动速度
- [ ] 4.3 创建怪物槽位表现数据结构或组件，暴露 9 个槽位锚点和后续目标选择所需定位点
- [ ] 4.4 在 `Game` 场景中挂载或实例化 `BattleStage`，并确保 `GameView` 与战斗舞台可以同时显示

## 5. 验证

- [ ] 5.1 在 Unity 编辑器中打开 `Game` 场景，验证场景、舞台和 `GameView` 都能正常显示
- [ ] 5.2 验证背景远景、中景、近景能播放一次前进滚动表现，且不依赖真实地图移动
- [ ] 5.3 验证 3x3 怪物槽位锚点位置、缩放和排序符合前中后层级预期
- [ ] 5.4 验证玩家操作 UI 位于 UI 层，背景和怪物位于 2D 世界空间层
- [ ] 5.5 运行可用的 EditMode 测试或 Unity 编译检查，确认新增脚本无编译错误
