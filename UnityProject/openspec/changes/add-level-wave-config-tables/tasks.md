## 1. 配置源目录确认

- [x] 1.1 确认 `D:\UnityGame\Self\RogueCard\Configs\GameConfig\Datas` 存在，并识别当前 Luban 配置源文件的格式和命名约定
- [x] 1.2 确认新增表文件应放在现有数据目录根级或项目约定的子目录中

## 2. 表结构创建

- [x] 2.1 新增波次类型枚举结构，包含 `Battle`、`Chest`、`Shop` 三类枚举值
- [x] 2.2 新增关卡主表结构，用于生成 `TbLevel`，字段包含关卡标识、名称、描述、默认入口标记和波次标识列表
- [x] 2.3 新增关卡波次表结构，用于生成 `TbLevelWave`，字段包含波次标识、所属关卡标识、顺序、波次类型、标题、描述、继续文案和可选负载标识
- [x] 2.4 保持新增表不填写正式玩法数据，只保留字段结构或必要的表头内容

## 3. 验证

- [x] 3.1 检查新增表文件路径、文件名和字段命名是否与现有配置源约定一致
- [x] 3.2 确认本次未新增怪物、卡牌、奖励、玩家初始状态或商店商品等非第一阶段配置表
- [x] 3.3 记录后续接入前需要补充 `level_default_001` 和对应波次数据

后续接入运行时前，需要在 `level.xlsx` 中补充 `level_default_001`，并在 `level_wave.xlsx` 中补充对应波次数据。
