## 1. 配置源目录确认

- [x] 1.1 确认 `D:\UnityGame\Self\RogueCard\Configs\GameConfig\Datas` 存在，并识别当前 Luban 配置源文件的格式和根级文件命名约定
- [x] 1.2 确认本变更新增表文件应放在现有数据目录根级，并通过 `__tables__.xlsx` 注册

## 2. 表结构创建

- [x] 2.1 新增怪物配置表结构，用于生成 `TbMonster`，字段包含怪物标识、名称、描述和资源标识
- [x] 2.2 新增战斗波次刷怪方案表结构，用于生成 `TbBattleWaveSpawn`，字段包含刷怪方案标识、名称、描述和刷怪批次标识列表
- [x] 2.3 新增刷怪批次表结构，用于生成 `TbBattleWaveSpawnBatch`，字段包含批次标识、所属刷怪方案标识、顺序、怪物标识和数量
- [x] 2.4 在 `__tables__.xlsx` 中注册 `monster.TbMonster`、`battle.TbBattleWaveSpawn` 和 `battle.TbBattleWaveSpawnBatch`
- [x] 2.5 保持新增表不填写正式玩法数据，只保留字段结构或必要的表头内容

## 3. 验证

- [x] 3.1 检查新增表文件路径、文件名和字段命名是否与现有 Luban 配置源约定一致
- [x] 3.2 确认刷怪批次表可通过顺序字段表达“前一批怪物死亡后进入下一批”的最小数据结构
- [x] 3.3 确认本次未新增运行时刷怪、怪物实体、血量、死亡事件、战斗结算、奖励、卡牌或商店商品等非配置表内容
- [x] 3.4 记录后续接入前需要补充默认怪物、默认刷怪方案和对应批次数据

后续接入记录：接入运行时刷怪前，需要补充默认怪物、默认刷怪方案和对应刷怪批次数据。
