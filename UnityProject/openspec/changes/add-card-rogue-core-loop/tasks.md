> 本文件是卡牌 Rogue 最小闭环的总路书和任务看板。这里的 checkbox 表示后续独立 OpenSpec 变更的推进状态，不表示本变更要直接修改代码。

## 1. 路书使用规则

- [ ] 1.1 确认本变更只作为任务看板，不通过 `/opsx:apply` 直接实施玩法代码
- [ ] 1.2 每个模块开工前创建独立 OpenSpec 变更，并在该变更内生成 proposal、spec、design 和 tasks
- [ ] 1.3 每完成一个独立模块后，回到本看板更新对应路线项状态

## 2. 第一阶段：入口与关卡骨架

- [x] 2.1 创建并完成主界面关卡入口变更，范围只包含默认关卡展示、开始按钮和进入流程请求
- [ ] 2.2 创建并完成关卡运行时上下文变更，范围只包含关卡配置引用、当前波次索引和关卡结果（变更：`add-level-runtime-context`，规划已完成，待实施）
- [ ] 2.3 创建并完成关卡波次推进变更，范围只包含打怪、宝箱、商店三类波次的分发和完成推进
- [ ] 2.4 创建并完成非战斗波次占位变更，范围只包含宝箱和商店的最小继续节点

## 3. 第二阶段：战斗规则核心

- [ ] 3.1 创建并完成怪物 3x3 阵型变更，范围只包含槽位模型、合法目标、死亡前移和转圈规则
- [ ] 3.2 创建并完成怪物行为最小框架变更（已被升级为 `refactor-monster-to-deck-driven`，行为对称 + 牌组驱动）
- [ ] 3.3 创建并完成玩家战斗状态变更（生命部分已拆出 `extend-player-level-base-stats`；防御/能量沿用 Change 1 的 IBattleActor）
- [ ] 3.4 创建并完成卡牌牌堆与回合变更，范围只包含抽牌堆、手牌、弃牌堆、洗牌、回合开始和结束回合
- [ ] 3.5 创建并完成卡牌出牌与效果变更（已被升级为 `rework-card-effect-model`，5 类卡 + Effect 组件 + Executor）

## 4. 第三阶段：成长与奖励

- [ ] 4.1 创建并完成击杀经验变更，范围只包含怪物死亡经验、升级阈值和等级提升判定
- [ ] 4.2 创建并完成三选一卡牌奖励变更，范围只包含候选生成、奖励选择和选择后加入牌组
- [ ] 4.3 创建并完成奖励暂停恢复变更，范围只包含战斗中升级暂停、选择奖励后恢复原流程
- [ ] 4.4 创建并完成宝箱奖励复用变更，范围只包含宝箱节点复用卡牌奖励或固定奖励

## 5. 第四阶段：UI 与验证闭环

- [ ] 5.1 创建并完成最小战斗 UI 变更，范围只包含玩家状态、手牌、怪物阵型、回合状态和结束回合输入
- [ ] 5.2 创建并完成目标选择与出牌输入变更（已被升级为 `polish-card-drag-feedback`，含失败提示、SingleManual 选目标、PendingCards 意图渲染、Buff 状态条）
- [ ] 5.3 创建并完成奖励选择 UI 变更，范围只包含三张候选卡牌展示和选择确认
- [ ] 5.4 创建并完成关卡结果反馈变更，范围只包含胜利、失败、宝箱、商店的最小反馈
- [ ] 5.5 为每个独立模块补齐对应 EditMode 测试或手动验证记录，避免只依赖总路书验收

## 6. 详细战斗系统拆分（2026-05-09 新增）

本阶段是对 3.x / 5.2 的具体实施拆分，4 个变更必须按依赖串行推进：

- [x] 6.1 `rework-card-effect-model`：卡牌效果模型重塑（TbCard + TbCardEffect + CardEffectExecutor + IBattleActor + 5 类基础卡）— 后续所有变更的前置
- [x] 6.2 `refactor-monster-to-deck-driven`：怪物行为对称化（卸载 monster-intent-config，新增 monster-deck-config，怪物有手牌/能量/抽牌堆+ AI Brain）— 依赖 6.1
- [x] 6.3 `extend-player-level-base-stats`：等级表新增 BaseHp 字段，BattleSystem 完全由表初始化玩家属性 — 与 6.1/6.2 互独立但建议串行
- [x] 6.4 `polish-card-drag-feedback`：UI 反馈（失败提示、目标选择、PendingCards 意图、Buff 状态条）— 依赖 6.1 + 6.2

## 7. 路书规则提醒

- 不要直接 `/opsx:apply` 本路书；应进入对应的独立变更目录后再 apply
- 6.1~6.4 完成并归档后，回来勾掉这里的 checkbox
